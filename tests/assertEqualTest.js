const assertEqual = require('../assertEqual');

const log = console.log;
// Test Code
log(assertEqual('Lighthouse Labs', 'Bootcamp'));
log(assertEqual(1, 1));
log(assertEqual('Johnny', 'Johnny'));
log(assertEqual(34, 3));